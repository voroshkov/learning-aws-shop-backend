{
  "scripts": {
    "build": "node build.js",
    "deploy": "serverless deploy --verbose",
    "build:deploy": "npm run build && npm run deploy",
    "test": "jest --verbose",
    "install:ImportFileParser": "cd src/ImportFileParser && npm i",
    "install:ImportProductsFile": "cd src/ImportProductsFile && npm i",
    "postinstall": "npm run install:ImportFileParser && npm run install:ImportProductsFile"
  },
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.17.16",
    "jest": "^29.5.0",
    "serverless-dotenv-plugin": "^6.0.0"
  }
}
